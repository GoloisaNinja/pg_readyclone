{
  "name": "pg_player",
  "version": "1.0.0",
  "description": "a super simple node express postgres react app as a template example for railway app deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "npm run start --prefix ./app/client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix ./app/client && npm run build --prefix ./app/client"
  },
  "author": "Jon Collins",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "pino": "^8.14.1",
    "sequelize": "^6.32.0"
  }
}
